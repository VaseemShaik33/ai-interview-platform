import { useEffect, useState } from "react";
import { Link, useParams } from "react-router-dom";
import { ArrowLeft, CheckCircle2, History, Sparkles, Target, TrendingUp } from "lucide-react";
import api from "../services/api";
import Loading from "../components/Loading";
import ErrorMessage from "../components/ErrorMessage";
import AppLayout from "../components/AppLayout";

export default function Result(){
 const {sessionId}=useParams(); const [result,setResult]=useState(null);const [error,setError]=useState("");
 useEffect(()=>{api.get(`/interviews/${sessionId}/details`).then(r=>setResult(r.data)).catch(e=>setError(e.response?.data?.message||"Could not load result"));},[sessionId]);
 if(!result&&!error)return <Loading text="Calculating result..."/>; if(error)return <AppLayout><main className="p-8"><ErrorMessage message={error}/></main></AppLayout>;
 return <AppLayout><main className="mx-auto max-w-[1200px] px-5 py-8 sm:px-8"><div className="flex items-center justify-between"><div><p className="text-sm font-medium text-violet-600">Interview Completed 🎉</p><h1 className="mt-1 text-3xl font-bold text-slate-900">Great job! Here’s your performance summary.</h1><p className="mt-2 text-slate-500">{result.category} · {result.difficulty}</p></div><Link to="/dashboard" className="hidden items-center gap-2 rounded-xl border border-slate-200 bg-white px-4 py-2.5 text-sm font-semibold text-slate-600 sm:flex"><ArrowLeft size={16}/> Dashboard</Link></div>
 <section className="mt-7 grid gap-5 lg:grid-cols-[300px_1fr]"><div className="rounded-2xl border border-slate-200 bg-white p-6 text-center shadow-sm"><p className="text-sm font-semibold text-slate-500">Overall Score</p><div className="mx-auto mt-5 flex h-44 w-44 items-center justify-center rounded-full" style={{background:`conic-gradient(#5b34e8 ${result.percentage}%, #eef2f7 0)`}}><div className="flex h-36 w-36 flex-col items-center justify-center rounded-full bg-white"><span className="text-4xl font-bold text-slate-900">{Math.round(result.percentage)}%</span><span className="mt-1 text-xs text-slate-400">{result.totalScore}/{result.maximumScore}</span></div></div><p className="mt-5 font-semibold text-emerald-600">Good Performance</p></div>
 <div className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="font-bold text-slate-900">Score Breakdown</h2><div className="mt-6 grid gap-5 sm:grid-cols-3"><Metric icon={Target} label="Questions" value={`${result.answeredQuestions}/${result.totalQuestions}`}/><Metric icon={TrendingUp} label="Score" value={`${result.totalScore}/${result.maximumScore}`}/><Metric icon={Sparkles} label="Percentage" value={`${Math.round(result.percentage)}%`}/></div><div className="mt-7 rounded-xl bg-slate-50 p-4"><div className="flex justify-between text-sm"><span className="text-slate-500">Interview status</span><span className="font-semibold text-emerald-600">{result.status}</span></div></div></div></section>
 <section className="mt-6 grid gap-5 lg:grid-cols-2"><FeedbackCard title="Strengths" items={result.strengths} good/><FeedbackCard title="Areas to Improve" items={result.areasToImprove}/></section>
 <section className="mt-6 rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="text-lg font-bold text-slate-900">Question-by-question feedback</h2><div className="mt-5 space-y-4">{(result.answers||[]).map((a,i)=><div key={a.questionId||i} className="rounded-xl border border-slate-100 p-4"><div className="flex items-start justify-between gap-4"><div><p className="text-xs font-semibold text-slate-400">Question {i+1}</p><p className="mt-1 font-semibold text-slate-800">{a.questionText}</p></div><span className="rounded-full bg-violet-50 px-2.5 py-1 text-xs font-bold text-violet-700">{a.score}/10</span></div><p className="mt-3 text-sm leading-6 text-slate-600">{a.feedback}</p></div>)}</div></section>
 <div className="mt-6 flex flex-wrap gap-3"><Link to="/history" className="flex items-center gap-2 rounded-xl border border-slate-200 bg-white px-5 py-3 font-semibold text-slate-700"><History size={17}/> Interview History</Link><Link to="/interview/start" className="rounded-xl bg-violet-600 px-5 py-3 font-semibold text-white">Start New Interview</Link></div>
 </main></AppLayout>
}
function Metric({icon:Icon,label,value}){return <div className="rounded-xl border border-slate-100 p-4"><Icon size={18} className="text-violet-600"/><p className="mt-3 text-xs text-slate-400">{label}</p><p className="mt-1 text-xl font-bold text-slate-900">{value}</p></div>}
function FeedbackCard({title,items,good}) {return <div className="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm"><h2 className="font-bold text-slate-900">{title}</h2><div className="mt-4 space-y-3">{(items?.length?items:["Keep practicing consistently"]).map((x,i)=><div key={i} className={`rounded-xl p-4 text-sm ${good?"bg-emerald-50 text-emerald-800":"bg-red-50 text-red-800"}`}>{x}</div>)}</div></div>}
